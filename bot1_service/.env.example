BOT_TOKEN=123456:telegram-bot-token
# Inside docker-compose use service host; for local dev change to http://localhost:8000/api/v1
SERVER_BASE_URL=http://server:8000/api/v1
SERVICE_TOKEN=raw-bot1-service-token
DASHBOARD_EMAIL=admin@example.com
DASHBOARD_PASSWORD=pass1234
# Optional; if left empty, catalog will use local fallback and dashboard login is skipped.
# DASHBOARD_EMAIL=
# DASHBOARD_PASSWORD=
DEFAULT_LANGUAGE=uz
CATALOG_CACHE_TTL=900
