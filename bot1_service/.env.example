# =========================
# BOT1 TELEGRAM SETTINGS
# =========================
BOT_TOKEN=123456:telegram-bot-token
DEFAULT_LANGUAGE=uz

# =========================
# CRM API SETTINGS
# =========================
# Local (Docker'siz): http://localhost:8000/api/v1
# Docker compose:     http://server:8000/api/v1
# Production:         https://api.example.com/api/v1
SERVER_BASE_URL=http://localhost:8000/api/v1

# Raw service token (NOT SHA256 hash). Backend checks hash of this value.
SERVICE_TOKEN=raw-bot1-service-token

# =========================
# OPTIONAL DASHBOARD AUTH
# =========================
# If provided, bot reads dynamic catalog from CRM via /auth/login + /catalog/items
# If empty, local fallback catalog is used.
DASHBOARD_EMAIL=admin@example.com
DASHBOARD_PASSWORD=pass1234

# Catalog cache TTL in seconds
CATALOG_CACHE_TTL=900
